# 高校学生公寓智能电表数据可视化系统

## 项目文档

---

## 一、项目概述

### 1.1 项目背景

随着高校后勤管理信息化进程加快，学生公寓用电管理已逐步引入智能电表设备。然而传统用电管理方式仍存在数据采集滞后、统计分析不足、异常用电难以预警、用电行为不透明等问题。本系统旨在构建一套集数据采集、统计分析、可视化展示、异常预警、账单管理于一体的智能电表数据可视化平台。

### 1.2 项目目标

- 设计并实现一套 B/S 架构的高校学生公寓智能电表数据可视化系统
- 支持学生、宿舍管理员、系统管理员三种角色协同工作
- 实现用电数据实时采集、统计排名、异常预警、图表展示、账单管理等核心功能
- 提供清晰直观的数据可视化界面，提升用电管理效率

### 1.3 项目目录

```
dormitory-energy-monitor/
├── backend/                          # Spring Boot 后端
│   ├── pom.xml                       # Maven 配置
│   └── src/main/
│       ├── java/com/dormitory/
│       │   ├── Application.java      # 启动类
│       │   ├── common/               # 通用类 (Result, LoginException)
│       │   ├── config/               # 配置 (WebConfig, MyBatisPlusConfig, GlobalExceptionHandler)
│       │   ├── entity/               # 实体类 (User, Dormitory, Meter, ElectricityRecord, Alert, Bill)
│       │   ├── mapper/               # MyBatis Mapper 接口
│       │   ├── service/              # 服务接口与实现
│       │   ├── controller/           # REST 控制器
│       │   ├── interceptor/          # JWT 登录拦截器
│       │   └── util/                 # 工具类 (JwtUtil)
│       └── resources/
│           ├── application.yml       # 应用配置
│           ├── init.sql              # 数据库初始化脚本
│           └── mapper/               # MyBatis XML (预留)
├── frontend/                         # Vue 3 前端
│   ├── index.html
│   ├── vite.config.js                # Vite 配置 (含代理)
│   └── src/
│       ├── main.js                   # 入口
│       ├── App.vue                   # 根组件
│       ├── style.css                 # 全局样式
│       ├── api/index.js              # Axios 封装 + 拦截器
│       ├── router/index.js           # Vue Router 路由
│       └── views/
│           ├── Login.vue             # 登录页
│           ├── Layout.vue            # 主布局 (侧边栏 + 顶栏)
│           ├── student/              # 学生端页面
│           │   ├── Dashboard.vue     # 仪表盘
│           │   ├── Usage.vue         # 用电图表
│           │   ├── Bills.vue         # 账单查询
│           │   └── Alerts.vue        # 预警信息
│           ├── admin/                # 宿管端页面
│           │   ├── Dashboard.vue     # 数据概览
│           │   ├── UsageQuery.vue    # 用电查询
│           │   ├── AlertManage.vue   # 预警处理
│           │   └── BillManage.vue    # 账单管理
│           └── system/               # 系统管理端页面
│               ├── Dashboard.vue     # 系统概览
│               ├── UserManage.vue    # 用户管理
│               ├── DormitoryManage.vue # 宿舍管理
│               └── MeterManage.vue   # 电表管理
```

---

## 二、技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 后端框架 | Spring Boot | 2.7.18 |
| ORM 框架 | MyBatis-Plus | 3.5.5 |
| 数据库 | MySQL | 5.7.35 |
| 连接池 | Druid | 1.2.20 |
| 认证 | JWT (jjwt) | 0.9.1 |
| 构建工具 | Maven | 3.0.5+ |
| 前端框架 | Vue 3 (Composition API) | 3.x |
| 构建工具 | Vite | 8.x |
| UI 组件库 | Element Plus | 2.x |
| 图表库 | ECharts | 5.x |
| 路由 | Vue Router | 4.x |
| HTTP 客户端 | Axios | 1.x |
| 语言 | Java / JavaScript | 8 / ES6+ |

---

## 三、系统架构

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────┐
│                    浏览器 / 移动端 H5                  │
├─────────────────────────────────────────────────────┤
│              Vue 3 前端 (Port 3000)                   │
│  ┌───────────┬──────────┬──────────────┐            │
│  │ 学生端     │ 宿管端    │ 系统管理端    │            │
│  │ Dashboard │ Dashboard│ UserManage   │            │
│  │ Usage     │ UsageQry │ DormManage   │            │
│  │ Bills     │ Alerts   │ MeterManage  │            │
│  │ Alerts    │ Bills    │              │            │
│  └───────────┴──────────┴──────────────┘            │
├─────────────────────────────────────────────────────┤
│            Axios + Vite Proxy → REST API              │
├─────────────────────────────────────────────────────┤
│           Spring Boot 后端 (Port 8080)                │
│  ┌──────────┬──────────┬──────────────────┐        │
│  │ Controller│ Service  │ Mapper (MyBatis) │        │
│  │ (REST)   │ (Logic)  │                  │        │
│  └──────────┴──────────┴──────────────────┘        │
│  ┌──────────────────────────────────────────┐      │
│  │  JWT Interceptor (登录认证)               │      │
│  └──────────────────────────────────────────┘      │
├─────────────────────────────────────────────────────┤
│              MySQL 5.7 (Port 3306)                   │
│  ┌──────────┬──────────┬────────────┬──────────┐   │
│  │ user     │ dormitory│ meter      │ bill     │   │
│  │ alert    │electricity│            │          │   │
│  │          │ _record  │            │          │   │
│  └──────────┴──────────┴────────────┴──────────┘   │
└─────────────────────────────────────────────────────┘
```

### 3.2 认证流程

```
Client                    Server
  │                          │
  │  POST /api/auth/login    │
  │  {studentNo, password}   │
  │─────────────────────────>│
  │                          │  校验账号密码
  │                          │  生成 JWT Token
  │  {token, user}           │  (含 userId, role)
  │<─────────────────────────│
  │                          │
  │  GET /api/student/xxx    │
  │  Authorization: Bearer   │
  │  <token>                 │
  │─────────────────────────>│
  │                          │  LoginInterceptor
  │                          │  解析 Token → userId, role
  │                          │  注入 request attribute
  │                          │
  │  Response                │
  │<─────────────────────────│
```

---

## 四、数据库设计

### 4.1 E-R 图

```
                    ┌──────────────┐
                    │    user      │
                    ├──────────────┤
                    │ id (PK)      │
                    │ student_no   │── unique
                    │ name         │
                    │ phone        │
                    │ password     │
                    │ gender       │
                    │ college      │
                    │ dormitory_id │── FK → dormitory.id
                    │ role         │── student/admin/super_admin
                    │ status       │
                    │ create_time  │
                    └──────┬───────┘
                           │ N:1
                    ┌──────▼───────┐
                    │  dormitory   │
                    ├──────────────┤
                    │ id (PK)      │
                    │ building_no  │
                    │ room_no      │── unique(building_no, room_no)
                    │ floor        │
                    │ capacity     │
                    │ occupants    │
                    │ meter_id     │── FK → meter.id
                    │ status       │
                    └──────┬───────┘
                           │ 1:1
                    ┌──────▼───────┐
                    │    meter     │
                    ├──────────────┤
                    │ id (PK)      │
                    │ meter_no     │── unique
                    │ dormitory_id │
                    │ current_usage│
                    │ voltage      │
                    │ current      │
                    │ power        │
                    │ frequency    │
                    │ threshold    │
                    │ status       │
                    │ last_time    │
                    └──────┬───────┘
                           │ 1:N
              ┌────────────┼────────────┐
      ┌───────▼──────┐ ┌──▼────────┐ ┌─▼──────────┐
      │electricity   │ │  alert    │ │   bill     │
      │  _record     │ ├───────────┤ ├────────────┤
      ├──────────────┤ │ id (PK)   │ │ id (PK)    │
      │ id (PK)      │ │ dorm_id   │ │ dorm_id    │
      │ meter_id     │ │ alert_type│ │ bill_month │
      │ dormitory_id │ │ alert_data│ │ usage_kwh  │
      │ usage_kwh    │ │ alert_time│ │ unit_price │
      │ voltage      │ │ status    │ │ amount     │
      │ current      │ │ note      │ │ pay_status │
      │ power        │ │ hand_time │ │ create_time│
      │ collect_time │ └───────────┘ └────────────┘
      └──────────────┘
```

### 4.2 数据表清单

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `user` | 用户表 (三种角色) | id, student_no, role, dormitory_id |
| `dormitory` | 宿舍表 | id, building_no, room_no, meter_id |
| `meter` | 电表表 | id, meter_no, dormitory_id, current_usage, alert_threshold |
| `electricity_record` | 用电记录表 | id, meter_id, dormitory_id, usage_kwh, collect_time |
| `alert` | 预警记录表 | id, dormitory_id, alert_type, status |
| `bill` | 账单表 | id, dormitory_id, bill_month, amount, pay_status |

### 4.3 初始数据

- 1 名系统管理员 + 2 名宿舍管理员 + 9 名学生
- 15 间宿舍 (3栋楼)
- 15 个电表
- 15个宿舍 × 30天 ≈ 450 条用电模拟记录
- 4 条预警记录 (2已处理, 2未处理)
- 90 条账单记录 (15个宿舍 × 6个月)

---

## 五、API 接口文档

### 5.1 认证接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/auth/login` | 用户登录 | 否 |
| GET | `/api/auth/info` | 获取当前用户信息 | 是 |

**登录请求:**
```json
POST /api/auth/login
{ "studentNo": "20210001", "password": "123456" }
```

**登录响应:**
```json
{
  "code": 200,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "user": {
      "id": 4, "studentNo": "20210001", "name": "王同学",
      "role": "student", "dormitoryId": 1, "college": "计算机学院"
    }
  }
}
```

### 5.2 学生端接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/student/dashboard` | 个人仪表盘 (用电概览、电表、预警、账单、建议) |
| GET | `/api/student/usage/chart?days=7` | 用电趋势图数据 (默认7天) |
| GET | `/api/student/usage/ranking?days=7` | 用电排行 |
| GET | `/api/student/alerts` | 本宿舍预警列表 |
| GET | `/api/student/bills` | 本宿舍账单列表 |

### 5.3 宿管端接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/dashboard` | 数据概览 (今日/本月用电、排行、预警数) |
| GET | `/api/admin/usage/query?dormitoryId=&days=7` | 用电数据查询 |
| GET | `/api/admin/usage/ranking?days=30` | 用电排行 |
| GET | `/api/admin/alerts?page=1&size=10` | 预警列表 (分页) |
| PUT | `/api/admin/alerts/{id}/handle` | 处理预警 `{ "handleNote": "已处理" }` |
| GET | `/api/admin/bills?page=1&size=10` | 账单列表 (分页) |
| POST | `/api/admin/bills/generate` | 生成当月账单 `{ "month": "2026-05" }` |
| GET | `/api/admin/dormitories` | 宿舍列表 |
| GET | `/api/admin/report?days=30` | 报表数据 |

### 5.4 系统管理端接口

**用户管理:**

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/system/users?page=1&keyword=` | 用户列表 |
| POST | `/api/system/users` | 新增用户 |
| PUT | `/api/system/users/{id}` | 编辑用户 |
| DELETE | `/api/system/users/{id}` | 删除用户 |
| PUT | `/api/system/users/{id}/status` | 切换启用/禁用 |

**宿舍管理:**

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/system/dormitories?page=1` | 宿舍列表 |
| POST | `/api/system/dormitories` | 新增宿舍 |
| PUT | `/api/system/dormitories/{id}` | 编辑宿舍 |
| DELETE | `/api/system/dormitories/{id}` | 删除宿舍 |

**电表管理:**

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/system/meters?page=1` | 电表列表 |
| POST | `/api/system/meters` | 新增电表 |
| PUT | `/api/system/meters/{id}` | 编辑电表 |
| PUT | `/api/system/meters/{id}/bind` | 绑定宿舍 `{ "dormitoryId": 1 }` |
| PUT | `/api/system/meters/{id}/threshold` | 设置预警阈值 `{ "threshold": 50 }` |

### 5.5 统一响应格式

```json
{
  "code": 200,      // 200 成功, 401 未登录, 500 错误
  "msg": "success", // 消息
  "data": {}        // 数据
}
```

---

## 六、快速开始

### 6.1 环境要求

| 软件 | 最低版本 |
|------|----------|
| JDK | 1.8+ |
| Maven | 3.0.5+ |
| Node.js | 16+ |
| MySQL | 5.7+ |

### 6.2 数据库初始化

```bash
mysql -u root -p < backend/src/main/resources/init.sql
```

或已在运行 MySQL 时直接执行 SQL 文件内容。默认配置:
- 用户名: `root`
- 密码: `123456`
- 数据库: `dormitory_monitor`

### 6.3 启动后端

```bash
cd backend
mvn spring-boot:run
```

后端启动后访问: `http://localhost:8080`

### 6.4 启动前端

```bash
cd frontend
npm install
npm run dev
```

前端启动后访问: `http://localhost:3000`

> 前端开发服务器已配置 API 代理，所有 `/api/*` 请求自动转发到后端 `http://localhost:8080`。

### 6.5 测试账号

| 角色 | 账号 | 密码 | 访问路径 |
|------|------|------|----------|
| 学生 | `20210001` | `123456` | 自动跳转学生仪表盘 |
| 宿管员 | `staff001` | `admin123` | 自动跳转宿管数据概览 |
| 系统管理员 | `admin001` | `admin123` | 自动跳转系统管理概览 |

---

## 七、页面说明

### 7.1 登录页

统一登录入口，三种角色使用同一登录页面。系统根据角色自动跳转到对应的工作台。

### 7.2 学生端

| 页面 | 功能 |
|------|------|
| 我的仪表盘 | 今日/本月用电、宿舍电表信息、近7天趋势图、最新账单、预警提醒、节能建议 |
| 用电图表 | 用电趋势折线图 (支持7/15/30天切换)、各宿舍排名柱状图、电表详细参数 |
| 账单查询 | 本宿舍历史账单列表 (月份、用电量、金额、缴费状态) |
| 预警信息 | 本宿舍预警时间线 (高用电/异常/设备故障、已处理/未处理) |

### 7.3 宿管端

| 页面 | 功能 |
|------|------|
| 数据概览 | 今日总用电、本月总用电、未处理预警数、宿舍总数、用电排行Top10、全楼趋势图 |
| 用电查询 | 按宿舍筛选、按时间段查询用电趋势折线图 |
| 预警处理 | 预警列表 (分页)、填写处理备注、确认处理 |
| 账单管理 | 账单列表 (分页)、一键生成当月账单 |

### 7.4 系统管理端

| 页面 | 功能 |
|------|------|
| 系统概览 | 用户/宿舍/电表总数统计、系统信息展示 |
| 用户管理 | 新增/编辑/删除用户、启用/禁用账号、学号查重 |
| 宿舍管理 | 新增/编辑/删除宿舍、楼栋/房间/楼层/容量管理 |
| 电表管理 | 绑定宿舍、设置预警阈值 (kWh/日) |

---

## 八、配置说明

### 8.1 后端配置 (`application.yml`)

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dormitory_monitor?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true   # 下划线转驼峰

jwt:
  secret: dormitory-energy-monitor-secret-key-2026
  expiration: 86400000                   # 24小时
```

### 8.2 前端配置 (`vite.config.js`)

```js
export default defineConfig({
  plugins: [vue()],
  server: {
    port: 3000,
    proxy: { '/api': 'http://localhost:8080' }  // API 代理
  }
})
```

---

## 九、项目特点

1. **前后端分离**: B/S 架构，RESTful API，便于维护和扩展
2. **三角色权限**: JWT 认证 + 角色路由隔离，不同角色看到不同菜单和功能
3. **数据可视化**: ECharts 折线图、柱状图，直观展示用电趋势和排名
4. **异常预警**: 自动检测超阈值用电，宿管可处理并填写备注
5. **模拟数据**: 预置 15 间宿舍、30 天用电记录、预警和账单，开箱即用
6. **模块化设计**: 后端 Controller-Service-Mapper 三层架构，前端组件化开发
